#include<iostream>
using namespace std;
int main() {
	int n;
	cin >> n;
	long long a = 0;
	long long b = 1;
	long long next;
	for (int i = 0;i < n;i++) {
		if (i == n - 1) {
			cout << a << endl;
		}
		next = a + b; 
		a = b; 
		b = next; 
	}
	return 0;
}
