#include <iostream>
#include <algorithm>
using namespace std;
int main() {
    int size, t;
    cin >> size >> t;
    long long arr[size];
    for (int i = 0; i < size; i++) {
        cin >> arr[i];
    }
    sort(arr, arr + size);
    while (t--) {
        long long key;
        cin >> key;

        long long start = 0, end = size - 1;
        bool flag = false;

        while (start <= end) {
            long long mid = (start + end) / 2;
            if (key == arr[mid]) {
                cout << "found" << endl;
                flag = true;
                break;
            }
            else if (key < arr[mid]) {
                end = mid - 1;
            }
            else {
                start = mid + 1;
            }
        }

        if (!flag) {
            cout << "not found" << endl;
        }
    }
    return 0;
}
