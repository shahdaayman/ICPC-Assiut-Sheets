#include<iostream>
#include<string>
using namespace std;
int main() {
	int t;
	cin >> t;
	while (t--) {
		string s;
		bool flag = false;
		cin >> s;
		for (int i = 0;i < s.size();i++) {
			if (s.substr(i, 3) == "010" || s.substr(i, 3) == "101") {
				flag = true;
				break;
			}
		}
		if (flag) {
			cout << "Good" << endl;
		}
		else {
			cout << "Bad" << endl;
		}
	}
	return 0;
}
