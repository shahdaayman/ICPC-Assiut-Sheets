#include<iostream>
#include<string>
using namespace std;
int main() {
	string s;
	int t;
	cin >> t;
	for (int i = 0;i < t;i++) {
		cin >> s;
		int size = s.size();
		if (s.size() > 10) {
			cout << s[0] << size-2 << s[size - 1] << endl;
		}
		else {
			cout << s << endl;
		}
	}
	return 0;
}
