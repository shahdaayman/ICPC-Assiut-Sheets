#include<iostream>
using namespace std;
int main() {
	string s;
	cin >> s;
	string word = "hello";
	int cnt = 0;
	for (int i = 0;i < s.size();i++) {
		if (word[cnt] == s[i]) {
			cnt++;
		}
	}
	if (cnt > 4) {
		cout << "YES" << endl;
	}
	else {
		cout << "NO" << endl;
	}
	return 0;
}
