#include<iostream>
using namespace std;
int main() {
	long long t,n,factorial=1;
	cin >> t;
	while (t--) {
           cin >> n;
		   factorial = 1;
		   for (int i = 1;i <= n;i++) {
			   factorial *= i;
		   }
    cout << factorial << endl;
	   }
	return 0;
}
